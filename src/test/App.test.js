import React from 'react';
import { shallow, mount } from 'enzyme';

import App from '../components/App';

describe('App', () => {

  it('should exist', () => {

  });

  it('should have an initial state', () => {

  });

  it('should render CurrentWeather component', () => {

  });

  it('should render a Search component', () => {

  });

  it('should render a SevenDay component', () => {

  })

  it('should render a TenDay component', () => {

  })

  it('should set a location for weather data', () => {

  })

  it('should render a welcome page if no location has been set', () => {

  })

  it('should persist location in local storage', () => {

  })
});