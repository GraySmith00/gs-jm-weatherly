const mockData = require("./mockData")

// console.log(mockData.current_observation.display_location.full  )

const city = mockData.current_observation.display_location.city

const currentCondition = mockData.current